#include<stdio.h>
#include<stdlib.h>
#include<string.h>
#include<limits.h>
// ps : i was unable to complete the 2nd code before submission time,so just putting this file for the sake of submission
int main(){
    int n;
    scanf("%d",&n);
   for(int i=0;i<2*n;i++){
    fgets(string,sizeof(string),stdin);
   }
   return 0;
}